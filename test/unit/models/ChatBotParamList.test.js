'use strict'
/* global describe, it */

const assert = require('assert')

describe('ChatBotParamList Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ChatBotParamList'])
  })
})
