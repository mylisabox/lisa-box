'use strict'
/* global describe, it */

const assert = require('assert')

describe('FavoritesController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['FavoritesController'])
  })
})
