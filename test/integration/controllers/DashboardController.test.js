'use strict'
/* global describe, it */

const assert = require('assert')

describe('DashboardController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['DashboardController'])
  })
})
