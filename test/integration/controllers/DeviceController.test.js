'use strict'
/* global describe, it */

const assert = require('assert')

describe('DeviceController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['DeviceController'])
  })
})
